<!DOCTYPE html>
<html lang="en">
  <head>
    <!-- Bootstrap CSS -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.2.1/css/bootstrap.min.css"
      integrity="sha384-GJzZqFGwb1QTTN6wy59ffF1BuGJpLSa9DkKMp0DgiMDm4iYMj70gZWKYbI706tWS"
      crossorigin="anonymous"
    />
    <title>URL - TinyApp Example</title>
    <style>
      .card-text {
        margin-top: 10px;
      }
    </style>
  </head>
  <body>
    <%- include('partials/_header') %>
    <div class="card text-center">
      <div class="card-header"></div>
      <div class="card-body">
        <h5 class="card-title">TinyURL for: <%= longURL %></h5>
        <!-- analytics for times visited and number of unique visitors -->
        
          <h6>Times visited: 
            <% if (times) { %>
              <%= times %>
            <% } else { %>
              <%= 0 %></h6>
            <% } %>
          <h6>Unique Visitors: 
            <% if (unique) { %>
              <%= unique %>
            <% } else { %>
              <%= 0 %></h6>
            <% } %>

        <!-- link to external site (from the long URL) -->
        <p class="card-text">
          Short URL ID: <a href="/u/<%= id %>">/u/<%= id %></a>
        </p>
      </div>

      <!-- link to make new links form -->
      <div class="card-footer text-muted"><a href = '/urls/new'>Make new shortened link!</a></div>
     
      <div>
        <!-- edit link form start -->
        <form method="POST" action= "/urls/<%= id %>?_method=PUT">
          <input type= url name = "new_URL" required>
          <button type="submit" class="btn btn-primary">Edit</button>
        </form>
        <!-- edit link form end -->
      </div>
    </div>
    <!-- table to log visitor type, partial id and time of visit -->
    <div>
      <table class="table table-info">
        <thead>
          <tr>
            <th scope="col">User Type</th>
            <th scope="col">User ID</th>
            <th class = "div1" scope="col">Time of Visit</th>
          </tr>
        </thead>
        <tbody>
          <% for (let each of visit) { %>
          <tr>
            <% if (each.status) { %>
            <td>User</td>
            <% } else { %>
            <td>Guest</td>
            <% } %>
            <td><%= each.tempId %></td>
            <td><%= each.date %></td>
            <% } %>
          </tr>
        </tbody>
      </table>
    </div>
    <script
    src="https://code.jquery.com/jquery-3.3.1.slim.min.js"
    integrity="sha384-q8i/X+965DzO0rT7abK41JStQIAqVgRVzpbzo5smXKp4YfRvH+8abtTE1Pi6jizo"
    crossorigin="anonymous"
  ></script>
  <script
    src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"
    integrity="sha384-UO2eT0CpHqdSJQ6hJty5KVphtPhzWj9WO1clHTMGa3JDZwrnQq4sF86dIHNDz0W1"
    crossorigin="anonymous"
  ></script>
  <script
    src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"
    integrity="sha384-JjSmVgyd0p3pXB1rRibZUAYoIIy6OrQ6VrjIEaFf/nJGzIxFDsf4x0xIM+B07jRM"
    crossorigin="anonymous"
  ></script>
</body>
</html>